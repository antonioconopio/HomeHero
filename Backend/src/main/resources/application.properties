spring.application.name=homeHero
server.servlet.context-path=/${spring.application.name}
server.port=8080

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# Exclude DB (for now at least)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

#Credentials
DB_USER=postgres.yszsamtouruajsjzavhp
DB_PASS=mLTCR445JRCl7MCP
DB_URL=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?user=postgres.yszsamtouruajsjzavhp&password=${DB_PASS}

# DB Config (SUPABASE)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver

supabase.jwt.secret=NEED TOGET
supabase.url=NEEED TO GET
supabase.service.key=NEED TO GET

# Optional: JPA/Hibernate settings if you?re using Spring Data JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl